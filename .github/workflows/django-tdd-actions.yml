name: Testing the Application
on: push

jobs:
  test_app:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Django unit tests
        run:
          pip3 install --user Django==3.2.9
          python3 manage.py test
